#!/usr/bin/env bash
# bin/list-all

# Lee los tags de GitHub, limpia el texto y quita la 'v' inicial (para que se vea limpio en mise)
git ls-remote --tags https://github.com/extism/cli.git | 
  grep -o 'refs/tags/.*' | 
  cut -d/ -f3- | 
  sed 's/^v//' | 
  grep -v '{}' | 
  sort -V